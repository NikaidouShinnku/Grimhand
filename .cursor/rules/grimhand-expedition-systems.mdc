---
description: Grimhand 远征经验、遗物与怪物技能池
globs: Assets/_Project/Scripts/Expedition/**/*,Assets/_Project/Scripts/Battle/**/*
alwaysApply: false
---

# 远征系统（经验 / 遗物 / 怪物池）

## 经验
- 局内升级：`CharacterProgression.XpRequiredForLevel` — 升到 Lv.N 需 `(N-2)×3+8` XP
- 战斗奖励：`CombatXpRules.Roll` — 按区域/精英/Boss 对照怪物组合表（洞穴 8–10 / 精英 14–20 / Boss25 等）
- 运行时：`ExpeditionEngine.OnBattleFinished` → `GrantXpToParty`

## 遗物
- 定义：`Expedition/RelicDatabase.cs`
- 每 20 层成长：`RelicGrowthRules` + `ExpeditionRunState.RelicGrowthTiers`；获得时按当前层数结算，跨层时 `SyncFloorGrowth`
- 战斗：`RelicDatabase.BuildModifiers(relicIds, growthTiers)` → `RunModifierSnapshot`

## 怪物技能池
- SO：`CharacterDefinitionSO.SkillPool`（2–4 张候选）
- 开战：`BattleConfig.UseRandomSkillPool` + `EnemyDeckBuilder.BuildRandomDeck`（种子 = battle seed）
- 内容生成：`Grimhand → Content → Generate Demo ScriptableObjects` 会跑 `MonsterContentGenerator`
- 立绘：`CharacterVisualCatalog_Demo` 映射 goblin/slime/skeleton/skeleton2/wraith/wraith2

## 阵型（有效前中后排）
- 物理槽位 `CombatantState.Slot` 不变（UI 站位、换位技能仍按物理格）
- 规则层 `PositionRules.GetEffectiveSlot`：同队存活者按物理 Front→Middle→Back 排序，依次占有效前/中/后排
- 影响：伤害倍率、目标 Reach、槽位技能解析、后排减伤%、遗物前排防/后排抽牌；死亡后 `CombatantDeathRules` 会 `RefreshAllDerivedStats`

## 远征 UI / 流程
- 背景：`BattleBackgroundView` + `BattleUiIconCatalogSO.CaveBackground`（低透明度）
- 战后：`VictoryRewards`（点击领金币/遗物/卡牌）→ `RouteSelect`（底部 `cave_path` 三门）→ 战斗或 `TreasureLoot`
- 路线：`ExpeditionNodeType.Combat` / `Treasure`（权重见 `ExpeditionConfig`）；事件/商店待接
- 上回合明细：左下 `note` 按钮（背包上方）→ `BattleTurnDetailPanelView`
- 绑定美术：`Grimhand → Content → Bind Expedition Art`

## 勿做
- 不要把遗物做成装备槽；全场被动直到 run 结束
- 单局战斗（无 Expedition）不显示经验条、不应用 RunModifiers
